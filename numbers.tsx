const num1 = 8
const num2 = 6
let val
//addition
val = num1 + num2
//subtraction
val = num1 - num2
//multiplication
val = num1 * num2
//division
val = num1 / num2
//modulus (Remainder)
val = num1 % num2
//Math object
val = Math.PI
val = Math.round(8.7)
val = Math.round(8.4)
val = Math.pow(8,2)
val = Math.sqrt(64)
val = Math.abs(-4)
val = Math.ceil(8.1)
val = Math.floor(8.1)
val = Math.min(2, 6, 1)
val = Math.max(2, 6, 1)

//Math.random() returns a random number between 0 (inclusives), and 1 (exclusive)
val = Math.random()
//Random between 1 to 5
val = Math.floor(Math.random() * 5 + 1)










console.log(val)